import React from 'react';
import Button from 'components/buttons/Button';

const List = () => {
  return (
    <div>
      list
      <Button link text="login" to="/login" />
    </div>
  );
};

export default List;
